import styled from 'styled-components';

import React from 'react'

const Home = (props) => {
    return (
        <Container>
            
        </Container>
    )
}

export default Home

const Container = styled.div``